Inside media I store all the graphical results (videos/images/GIFs) related to the project.
This is the content I have:
- **Franka_levanta_cubo.gif**: GIF of the training result of a lift policy (just one franka arm), that training now does not work, but I have the visual result. It was trained with *joint_pos... .py* (starting from close position to red cube), script can be found inside the repo. 
- **FULL_PIPELINE.webm**: Demonstration of full pipeline in the "easiest" environment as possible. Lift red cube and deliver it in the red pallet.![[Scenario.png]] (photo of the easiest environment, no rotation nor offsets)
-  **FULL_WITH_VARIATION.webm**: Demonstration of complete pipeline (all 3 cubes), it applies cube rotation + one cube on top of another object. (still hardcoded + pure controller)
- **VLM_Results**: results of the VLM integration already inside *IsaacSim*, will need to analyze in the further documentation.